# ============================================================
# Requirements — Clasificación de Dialectos + XAI
# Modelo: cardiffnlp/twitter-xlm-roberta-base + LoRA
# Python 3.10+ / CUDA 12.x / RTX 3050 6GB
# ============================================================

# ── Core ML / Transformers ──
# IMPORTANTE: instalar PyTorch primero con el comando de abajo,
# NO desde este archivo directamente.
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
transformers==4.51.3
datasets==3.6.0
evaluate==0.4.3
accelerate==1.6.0

# ── LoRA / PEFT ──
peft==0.15.2

# ── Optimizador 8-bit (paged_adamw_8bit) ──
bitsandbytes==0.45.5

# ── Explicabilidad ──
captum==0.7.0
shap==0.46.0
lime==0.2.0.1

# ── Data / Análisis ──
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.1
scipy==1.15.3
pyarrow==20.0.0

# ── Visualización ──
matplotlib==3.10.1
seaborn==0.13.2
wordcloud==1.9.4

# ── NLP / Texto ──
nltk==3.9.1

# ── Utilidades ──
tqdm==4.67.1